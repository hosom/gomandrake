#!/usr/bin/python 

import json

from pluginbase import Plugin

def analyze(fmeta):
	fm = json.loads(fmeta)
	return '{"Hello from Python" : "%s"}' % fm['Filepath']

def main():

	plug = Plugin('NewHelloWorldPython')
	plug.listen(analyze)

if __name__ == '__main__':
	main()